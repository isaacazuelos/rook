module MovementBasicSpec (spec) where

import Test.Hspec
import Test.QuickCheck

import qualified Game.Rook.Movement.Basic as Basic
import qualified Game.Rook.Coord as Coord
import qualified Game.Rook.Mask as Mask

spec = do
  describe "from" $ return ()
  describe "to" $ return ()
  describe "capture" $ return ()
  describe "apply" $ return ()
  describe "step" $ return ()
  describe "cast" $ return ()
